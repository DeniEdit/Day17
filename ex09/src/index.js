const _ = required('lodash');

function getUsers() {
    getUsers 
}

function findUserById(id) {
    try {

    } catch (error) {
        return 
        console.log();
    }
}

getUsers();
findUserById();
module.exports = findUserById;